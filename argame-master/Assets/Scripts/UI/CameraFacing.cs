
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraFacing : MonoBehaviour {
/*
    Camera refCamera;
    public bool reverface = false;
    private Transform mRoot;
    private void Awake() //need to innt as it is used early in the scene
    {
        if (!refCamera)
        {
            refCamera = Camera.main; //let refCamera refer to  maincamera
        }
        mRoot = transform; //make mRoot be obj's transform
    }

    private void Update()
    {
        Vector3 targetPos = mRoot.position + refCamera.transform.rotation * (reverface ? Vector3.back : Vector3.forward); //vector3 include posion and rotation ,they can be combined by +
        Vector3 targetOrientation = refCamera.transform.rotation * Vector3.up;
        mRoot.LookAt(targetPos, targetOrientation);
    }
*/
}
